#include <stdio.h>

struct name{
    char name[50];
    int no;
}

void main(){
    int n;
    prin
    for(int i=0;i<5;i++){
        printf("name:");
        scanf("%s",&s[i].name);
        printf("number:");
        scanf("%d",&s[i].no);
    }
    printf("\n");
    for(int i=0;i<5;i++){
        printf("%s-",s[i].name);
        printf("%d\n",s[i].no);
    }
}