#include <stdio.h>
#include <stdlib.h>

/*Program to make Linked Lists

Linked List-unlike array,all the elements of the list are located at different places
the elements of the list are connected together by a pointer that points to the next element

There are two types of linked lists-singly and doubly linked list

singly-contains one pointer that points to the next element
doubly-contains two pointer that points to the next element and preivous element
*/


void main()
{   int n,;
    printf("Enter the number of elements:");
    scanf("%d",&n);

}